{
  "name": "borehole-log-studio",
  "private": true,
  "version": "0.5.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:web": "node scripts/web-regression.mjs",
    "test:source": "node scripts/source-check.mjs",
    "check": "npm run test:web && npm run test:source && npm run build",
    "tauri": "tauri",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build"
  },
  "dependencies": {
    "pdfjs-dist": "^4.10.38",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.5",
    "vite": "^6.0.7"
  }
}
