{
  "name": "borehole-log-studio-desktop",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "node scripts/copy-ocr-assets.mjs && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "desktop:dev": "npm run prepare:ocr && tauri dev",
    "desktop:build": "npm run prepare:ocr && tauri build",
    "prepare:ocr": "node scripts/copy-ocr-assets.mjs",
    "test:source": "node scripts/source-check.mjs"
  },
  "dependencies": {
    "@tesseract.js-data/eng": "^1.0.0",
    "pdfjs-dist": "^4.10.38",
    "tesseract.js": "^5.1.1",
    "tesseract.js-core": "^5.1.1",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.2.5",
    "vite": "^6.0.7"
  }
}
